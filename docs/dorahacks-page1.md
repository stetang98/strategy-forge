# DoraHacks BUIDL — Page 1 (Profile) fill guide

Copy each value into the matching field.

## BUIDL (project) name
```
Strategy Forge
```

## BUIDL logo
Upload `logo.png` (repo root) — 480×480 PNG, 27 KB. ✅ meets the spec.

## Vision — "Describe the problem which this project solves"
```
Crypto traders and AI-agent builders have no fast, trustworthy way to validate a
strategy idea. A rigorous backtest normally means writing quant code, sourcing data,
and dodging subtle look-ahead bias — so most "backtests" are cherry-picked and
impossible to reproduce.

Strategy Forge is a CoinMarketCap Agent-Hub Skill that turns a plain-English strategy
idea into a structured, backtestable spec and runs a walk-forward, cost-adjusted,
look-ahead-safe backtest — reproducible with zero API keys. Idea to evidence in
seconds: equity curve, Sharpe/Sortino/max-drawdown, and an honest out-of-sample
verdict vs buy-and-hold. Live CoinMarketCap signals (Fear & Greed, dominance, price)
annotate every run, and the same spec can later drive a live trading agent.
```

## Category
```
加密/Web3   (Crypto/Web3)
```
> Either "加密/Web3" or "人工智能/机器人技术" (AI) is defensible — it's a crypto tool
> authored as an LLM Skill. I recommend **加密/Web3**.

## Is this BUIDL an AI Agent?
```
No  (不)
```
> Honest answer for **Track 2 — Strategy Skills**: it's a strategy-generation *Skill*
> (a backtestable tool an agent uses), not an autonomous trading agent. Pick "Yes" only
> if you want to emphasize the LLM-Skill angle — but "No" matches the Track 2 deliverable.

## GitHub *
```
https://github.com/stetang98/strategy-forge
```

## Project website (optional)
> Leave blank (the GitHub repo is the home).

## Demo video *  ⚠️ REQUIRED — you must record + upload to YouTube
> This is the one field we can't fill yet. It needs a YouTube link.
> Ask me for the **60–90s demo video script** and I'll write it so you can record it in
> one take (run `market_context.py` for the live CMC read, then `make demo` for the CAKE
> "+39% vs −93%" chart). Then paste the YouTube URL here.

## Social links (at least one)
```
https://x.com/Stetang3438
https://github.com/stetang98
```

---
### Status
- ✅ name · logo · vision · category · AI-agent · GitHub · social — ready to paste
- ⚠️ **Demo video** — record + upload to YouTube first (ask me for the script)
