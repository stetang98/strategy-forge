# Optional — only needed to enable the CoinMarketCap-branded live provider + MCP demo.
# The backtest itself runs fully keyless (binance.vision + alternative.me), so this
# is NOT required to reproduce results.
#
# Get a free Basic key at https://pro.coinmarketcap.com/signup
CMC_API_KEY=
